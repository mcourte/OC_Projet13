"""
lettings/apps.py

Configuration for the 'lettings' application.

This module contains the configuration for the Django application 'lettings',
which manages functionalities related to properties in the project.
"""
from django.apps import AppConfig


class LettingsConfig(AppConfig):
    """Configuration class for the 'lettings' application.

    This class configures the 'lettings' application, which handles
    functionalities related to properties in the Django project.
    """
    name = 'lettings'
